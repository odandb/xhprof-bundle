imports:
    - { resource: services.yaml }

framework:
    secret: '123456789'
    http_method_override: false
    router:
        utf8: true
        resource: '%kernel.project_dir%/routing.yaml'
        strict_requirements: ~
    test: ~
    profiler:
        collect: false
